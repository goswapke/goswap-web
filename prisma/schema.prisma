// prisma/schema.prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

enum ListingType {
  LEASE
  SWAP
}

model Vehicle {
  id            String      @id @default(cuid())
  make          String
  model         String
  year          Int
  fuelType      String?
  transmission  String?
  locationCity  String
  imageUrl      String?
  listingType   ListingType @default(SWAP)
  description   String?
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt
}
